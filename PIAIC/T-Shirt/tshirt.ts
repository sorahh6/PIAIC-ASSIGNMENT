// T-Shirt: Defining a function make_shirt() and calling it

// Define the make_shirt function
function make_shirt(size: string, message: string): void {
    console.log(`You've ordered a ${size} shirt with the message: "${message}".`);
}

// Call the make_shirt function with sample values
make_shirt('Large', 'Hey People!');
